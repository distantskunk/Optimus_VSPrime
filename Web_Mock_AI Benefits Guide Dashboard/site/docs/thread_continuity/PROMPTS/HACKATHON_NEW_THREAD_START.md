Please read and execute the attached HACKATHON_NEW_THREAD_START.md.

Also read:

- HACKATHON_CONTINUITY.md
- HACKATHON_DECISIONS.md

Treat those files as the authoritative project baseline for this conversation.

Use the continuity file to reconstruct:

- current project objective
- current project state
- current implementation focus
- current MVP scope
- stable project decisions
- known risks
- immediate next task

Use the decision log to reconstruct:

- accepted decisions
- deferred decisions
- rejected ideas
- architecture decisions
- demo strategy decisions
- scope decisions

Do not rely on hidden memory, previous conversations, or assumptions.

Do not recreate project history.

Do not reopen accepted decisions unless:

- the user explicitly requests it
- a verified contradiction exists
- a blocking implementation issue exists

Do not expand MVP scope without justification.

Do not begin implementation until startup intake is complete.

When reviewing project state:

- identify the current phase
- identify the current critical path
- identify the immediate next action

If continuity and decision files conflict:

- identify the conflict
- identify implementation impact
- recommend which source should become authoritative

After processing all files, provide the Startup Completion Response defined in HACKATHON_NEW_THREAD_START.md.

Then stop and wait for instructions.